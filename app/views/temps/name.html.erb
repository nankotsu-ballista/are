<h1>テンプレートを登録</h1>
ヒーローとシチュエーションを合わせてテンプレートとして登録します。
<br>テンプレート集の名前と画像を決めてください。
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with(model: @newtemp, url: set_tempting_false_users_path, method: :post) do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.label :image %>
      <%= f.file_field :image %>

      <%= f.submit "登録", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>